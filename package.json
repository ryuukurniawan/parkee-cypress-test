{
  "name": "parkee-cypress-test",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "clean:reports": "rimraf cypress/reports",
    "pretest": "npm run clean:reports",
    "test": "npx cypress run",
    "posttest": "npx mochawesome-merge cypress/reports/mocha/*.json > cypress/reports/report.json && npx marg cypress/reports/report.json --reportDir cypress/reports --inline"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "cypress": "^15.14.0",
    "mocha": "^11.7.5",
    "mochawesome": "^7.1.4",
    "mochawesome-merge": "^5.1.1",
    "mochawesome-report-generator": "^6.3.2",
    "rimraf": "^6.1.3"
  }
}
